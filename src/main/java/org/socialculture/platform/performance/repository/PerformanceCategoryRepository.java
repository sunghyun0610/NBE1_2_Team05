package org.socialculture.platform.performance.repository;

import org.socialculture.platform.performance.entity.PerformanceCategoryEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PerformanceCategoryRepository extends JpaRepository<PerformanceCategoryEntity, Long> {
}
