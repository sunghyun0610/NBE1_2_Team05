<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} -%kvp- %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO"> <!-- 전체 로그 레벨을 INFO로 설정 -->
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
